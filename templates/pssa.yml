J:\PA:
  "{{ admin }}":
    Test Construction:
      Form Scrambling:
        Math: &scrambling-tree
          ? From TD
          ? Modified Templates
          ? Output to TD
        ELA: *scrambling-tree
        Science: *scrambling-tree
    _SpringAnalysis:
      Math: &content-tree
        Data Files:
          ? Calibration
          ? Daily
          ? Field Test
          ? Key Validation
        Equating:
          ? Calibration
          ? Field Test
          ? Key Validation
        Item Analysis:
          ? Calibration Data
          ? Field Test
          ? Key Validation
        ? Item Banking
        ? Winsteps
      ELA: *content-tree
      Science: *content-tree
    
  "{{ admin[0] }}00s":
    Tech Reports:
      Ben:
        ? plots
        ? tables
        ? spss syntax
      Appendices:
        ? to PUBS
      Chapters:
        ? ready for internal review
        ? ready for text update
        ? to PUBS
      Data Files:
        ? PDE
        ? PS
      ? Final
      ? from EIS
      ? from PAS
      ? from PUBS
  
{{ SYSTEMDRIVE }}\PA\PSSA\EPA{{ admin }}:
  ? DOTS
  ? notebooks
  ? plots
  ? tables
  ? data
  Tech Report:
    Data:
      ? dummy_pfiles
    ? plots
    ? tables
    ? Decision Consistency
    ? Invariance
  SpringAnalysis:
    ? data
    ? ELA
    ? MATH
    ? SCIENCE
    ? FinalEquating
