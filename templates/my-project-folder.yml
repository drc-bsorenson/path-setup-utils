{{ PS_DRIVE }}\{{ state }}\{{ project }}\Ben: &my-tree
  code:
     ? notebooks
     scripts:
        ? python
        ? R
        ? SQL
        ? SPSS
  output:
    ? tables
    ? plots
    ? item order
  ? documentation
  ? _test

{{ PROJ_HOME }}\{{ state }}\E{{ state }}{{ project }}: 
  << : *my-tree
  ? data
  ? DOTS
