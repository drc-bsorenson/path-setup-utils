J:\{{ state }}\{{ project }}\Ben: &my-tree
  code:
     ? notebooks
     scripts:
        ? python
        ? R
        ? SQL
        ? SPSS
  output:
    ? tables
    ? plots
  ? documentation
  ? _test

{{ SYSTEMDRIVE }}\{{ state }}\{{ project }}: 
  << : *my-tree
  ? data
